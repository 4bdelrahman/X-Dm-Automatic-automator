# X.com Credentials
X_USERNAME=your_x_username
X_PASSWORD=your_x_password

# Server
PORT=3000

# DM Settings
MAX_DMS_PER_DAY=30
MIN_DELAY_SECONDS=120
MAX_DELAY_SECONDS=600
SEND_HOURS_START=9
SEND_HOURS_END=21

# Mode: "live" sends real DMs, "dry-run" only logs
MODE=dry-run
