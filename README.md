# X Cold DM Bot — Cloud Deployment

## Architecture
- **Backend (Railway):** Express API + Playwright browser automation
- **Frontend (Vercel):** Next.js dashboard

## Setup
See PHASE 6 and PHASE 7 in the deployment prompt.

## Environment Variables
Copy `.env.example` files in `backend/` and `frontend/` to `.env` and fill values.
